# TODO

## Code Quality

- [ ] Add code coverage enforcement
  - Configure pytest-cov with `--cov-fail-under` threshold (e.g., 80%)
  - Add coverage configuration to pyproject.toml or .coveragerc
  - Integrate coverage reporting into CI pipeline
  - Reference: similar setup intended for ../daveseats
